{
  "name": "web-to-mcp",
  "description": "Web-to-MCP bridge for connecting to remote MCP endpoints",
  "config": {
    "type": "http",
    "url": "${WEB_TO_MCP_URL}"
  },
  "prompts": [
    {
      "var": "WEB_TO_MCP_URL",
      "message": "Full URL of the Web-to-MCP endpoint",
      "default": "",
      "sensitive": false
    }
  ]
}
